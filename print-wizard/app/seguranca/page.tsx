export default function PrivacidadePage() {
  return (
    <main className="max-w-3xl mx-auto px-6 py-12 text-sm leading-relaxed space-y-4">
      <h1 className="text-xl font-semibold">Política de Privacidade</h1>
      <pre className="whitespace-pre-wrap">
{`SEGURANÇA DA INFORMAÇÃO — PVTY

A segurança das informações dos usuários é prioridade na PVTY.

- Utilizamos criptografia nas comunicações (HTTPS).
- Monitoramos acessos e atividades suspeitas.
- Restringimos o acesso interno aos dados apenas a pessoas autorizadas.
- Mantemos backups regulares e protegidos.

Em caso de qualquer incidente de segurança, o usuário será notificado conforme exigido pela legislação.
`}
      </pre>
    </main>
  )
}
